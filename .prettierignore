.voder/

.github/**

# Ignore Husky hooks
.husky/**

test/cli.json.skip-outdated.test.js
scripts/validate-traceability.cjs

# Workspace state / generated artefacts — not owned by the project's
# formatting policy. Keeps `prettier --check .` (pre-commit per ADR-0013)
# scoped to source-controlled files only.
.risk-reports/
.afk-run-state/
.claude/
*.backup
# updates.json: auto-update.yml writes `dry-aged-deps --check --format=json`
# here as a run-scratch artefact consumed by later steps. Untracked and
# unformatted; without this ignore the pre-commit `format:check` fails the
# workflow's "Commit initial bump" step. Also .gitignore'd (never committed).
updates.json