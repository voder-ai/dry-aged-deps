<?xml version="1.0" encoding="UTF-8"?>
<traceability>
  <specification>prompts/013.0-DEV-CHECK-MODE.md</specification>
  <status>PASSED</status>
  <last_validated>2025-11-12T17:32:14.299Z</last_validated>
  <last_modified>2025-11-11T08:04:07.352Z</last_modified>
  <evidence>- bin/dry-aged-deps.js implements `--check` flag (line 22) and uses it to set `returnSummary: checkMode`, exiting with code 1 if `summary.safeUpdates > 0`, 0 otherwise, and 2 on errors (lines 557–567).
- CLI help (`--help`) documents `--check` flag.
- test/cli.check-mode.test.js covers:
  • exit code 0 when no updates in JSON mode
  • exit code 1 when safe updates in table, JSON, XML modes
  • exit code 2 on invalid format
- test/docs/ci-integration.test.js verifies README.md has a CI/CD Integration section with `npx dry-aged-deps --check` and `if: failure()`, and docs/api.md describes exit codes (0,1,2) under a CI/CD Integration heading.
- `npm test` (Vitest) ran 89 tests including cli.check-mode and docs integration, all passing.</evidence>
  <notes>All acceptance criteria for 013.0-DEV-CHECK-MODE are satisfied: the `--check` flag exists, correct exit codes (0,1,2) are returned in all formats, clear output is shown, JSON/XML modes supported, and documentation updated with examples.</notes>
</traceability>