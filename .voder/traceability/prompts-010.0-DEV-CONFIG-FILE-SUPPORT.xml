<?xml version="1.0" encoding="UTF-8"?>
<traceability>
  <specification>prompts/010.0-DEV-CONFIG-FILE-SUPPORT.md</specification>
  <status>PASSED</status>
  <last_validated>2025-11-12T20:56:19.333Z</last_validated>
  <last_modified>2025-11-10T21:23:22.067Z</last_modified>
  <evidence>All 7 tests in test/cli.config-file.test.js passed under Vitest, verifying config file reading (default .dry-aged-deps.json and custom file via --config-file), JSON parsing and structure validation (invalid JSON and unknown keys exit code 2 with clear messages), merging logic (defaults ← config ← CLI flags), and format support. The implementation in bin/dry-aged-deps.js reads and validates `.dry-aged-deps.json`, applies thresholds and format from config, and correctly honors CLI overrides. README.md includes a detailed example and documents the --config-file option.</evidence>
  <notes>The config-file support meets every acceptance criterion: correct location, schema validation, override precedence, error handling, optional usage, documentation, and sensible defaults. The JSON schema file is not present, but schema support was marked optional.</notes>
</traceability>