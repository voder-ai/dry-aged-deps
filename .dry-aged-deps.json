{
  "exclude": {
    "typescript": "Deferring TypeScript 5 → 6 major upgrade pending deliberate compatibility check against the JSDoc + checkJs setup in tsconfig.json (ADR-0006). Re-evaluate when scheduling a dedicated TS6 upgrade commit."
  }
}
