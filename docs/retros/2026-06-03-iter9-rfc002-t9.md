# Retro — AFK iter 9 (RFC-002 T9 ship-signal + verifying transition)

**Date**: 2026-06-03
**Iter scope**: Final RFC-002 work-unit. Two work-units bundled into one iter per ADR-014 single-commit grain (one commit each):

1. **Work-unit 1** — `feat(exposure-aware-soak): expose --exposure-aware-soak in README What's new for RFC-002 ship-signal` (commit `07311a1`). One-line "What's new" pointer added after `## ✨ The Solution`, anchoring to the existing `### Exposure-aware soak` body section (T6 `a83ccf3`). Shape (ii) — README-discoverability addition — per CLAUDE.md §"Multi-iter feature ship-signal" (codified yesterday in `db9cf5d` from RFC-001 experience).
2. **Work-unit 2** — `docs(rfcs): transition RFC-002 in-progress → verifying — exposure-aware soak ready for user-side check` (commit `d5ff125`). `git mv` rename, frontmatter `status:` flip, all 8 tasks marked `[x]` with originating commit SHAs, new `## Verification` section authored (release marker citing `07311a1` + projected v2.12.0; user-side check with `--exposure-aware-soak` + default-OFF opt-in boundary check + sibling-package preservation check); P014 `## RFCs` reverse-trace status flipped accepted → verifying via `wr-itil-update-problem-rfcs-section`; `docs/rfcs/README.md` first emit with both RFC-001 + RFC-002 in Verification Queue.

**Branch state**: 2 commits ahead of origin (do NOT push — orchestrator owns Step 6.5). When the orchestrator drains, semantic-release will mint v2.11.1 → v2.12.0 from `07311a1`.

## Gate stack

| Gate                                     | Work-unit 1     | Work-unit 2               |
| ---------------------------------------- | --------------- | ------------------------- |
| Architect                                | PASS_WITH_NOTES | n/a (mechanical)          |
| JTBD                                     | PASS            | n/a (mechanical)          |
| Voice & tone (README copy)               | PASS_WITH_NOTES | n/a (no user-facing copy) |
| External-comms risk (commit msg)         | PASS            | PASS                      |
| External-comms voice & tone (commit msg) | PASS            | PASS                      |
| Pipeline risk                            | 2/25 (Very Low) | 2/25 (Very Low)           |

Architect Note 1: defer meta-ADR codifying "What's new" vs "Options table row" as parallel ship-signal surfaces — premature on n=2 (RFC-001 used (ii), RFC-002 forced into a sibling variant because T6 consumed the table-row surface as `docs:`). Per inverse-P078 / P132 guard. **Accepted** — n=3 evidence not yet available; CLAUDE.md §"Multi-iter feature ship-signal" already hedges with "equivalent user-facing surface" language.

Architect Note 2: placement after `## ✨ The Solution` reads better than the badge-vs-Problem gap. **Actioned inline** — final placement after the Solution block.

Voice & tone optional refinements: "sitting on" → "npm audit reports... in your installed tree". **Declined** — preserves prose-parity with the existing body section at README.md:193 (which uses "sitting on a known-vulnerable version" verbatim).

## Pipeline Instability (Step 2b)

Two friction signals observed; both dedup against existing tickets — surfaced for orchestrator review per the AFK fallback (ADR-013 Rule 6). No new tickets created; evidence-append for the orchestrator to action via `/wr-itil:manage-problem` on its drain.

| Signal                                                                                                                                                                                                                                                                                                                                                                                                                           | Category                  | Citations                                                                                                                                                                                                                                                                      | Dedup                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Commitlint `subject-case` rejected the Work-unit 2 commit attempt on `docs(rfcs): RFC-002 in-progress → verifying — ...` (uppercase-led subject after the `:` — `RFC-002` triggered the rule). Fixed by re-leading with verb `transition` (`docs(rfcs): transition RFC-002 ...`). The skill's example transitional commit messages do not enforce verb-first in the template — agent inference produced the uppercase-led shape. | Skill-contract violations | `/wr-itil:manage-rfc` Step 7 transition-mechanics block + commitlint output `subject must not be sentence-case, start-case, pascal-case, upper-case [subject-case]`; observed at Bash invocation 19 attempting commit; resolved at Bash invocation 20 with verb-first subject. | **Matches P010 (known-error)** — `skill-md-commit-message-convention-fails-commitlint-subject-case`. Evidence-append candidate: this iter is the second observed instance under `docs(rfcs):` scope (prior P010 evidence was under different skill contexts). |
| External-comms gate marker re-keyed when the commit subject changed (uppercase → verb-first) — even though the body was unchanged. Required a second external-comms risk PASS + voice-tone PASS round-trip before the commit could proceed. Non-blocking but visible cost (~2 subagent invocations).                                                                                                                             | Hook-protocol friction    | `git commit` rejection on `BLOCKED (external-comms gate / voice-tone evaluator)` after subject-only edit; resolved by re-spawning both external-comms subagents with the new subject + identical body.                                                                         | **Matches P023 (open)** — `external-comms-gate-marker-re-hashes-on-every-draft-body-delta`. Evidence-append candidate: this iter is direct evidence of the re-fire pattern (subject delta with body-unchanged → marker re-hash → second-pass cost).           |

Both signals are pipeline-instability friction with concrete existing tickets — the trust-boundary recurring-class-of-behaviour classification applies; mechanical-auto-evidence-append per ADR-044, deferred to orchestrator drain per ADR-013 Rule 6 AFK fallback.

**Third near-signal (suppressed)**: pre-commit prettier reformatted `docs/rfcs/README.md` (widened the "Likely verified?" table column) between the initial `git add` + commit attempt — left the working tree dirty post-commit-failure. Resolved by re-staging in the next attempt. This is **expected behaviour** per ADR-0016 (pre-commit auto-format-and-restage); the dirty working tree was a transient artefact of the commitlint failure rolling back the stage, not a prettier defect. Not a ticket.

## Context Usage (Cheap Layer)

| Bucket             | Bytes                                               | % of total | Δ vs prior                       |
| ------------------ | --------------------------------------------------- | ---------- | -------------------------------- |
| memory             | 389,312                                             | 42.0%      | not measured — no prior snapshot |
| decisions          | 270,092                                             | 29.1%      | not measured — no prior snapshot |
| problems           | 189,425                                             | 20.4%      | not measured — no prior snapshot |
| jtbd               | 42,306                                              | 4.6%       | not measured — no prior snapshot |
| briefing           | 29,265                                              | 3.2%       | not measured — no prior snapshot |
| project-claude-md  | 7,747                                               | 0.8%       | not measured — no prior snapshot |
| hooks              | not measured — source-absent                        | —          | —                                |
| skills             | not measured — source-absent                        | —          | —                                |
| framework-injected | not measured — framework-injected-no-on-disk-source | —          | —                                |
| **THRESHOLD**      | 10,240                                              | —          | —                                |

Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer).

**README inventory currency advisory**: `packages dir not found: packages` — script gracefully degrades because dry-aged-deps is an adopter repo (no `packages/` plugin tree). One-line pointer logged per fail-open contract; not a ticket.

## Verification Candidates (Step 4a)

No `.verifying.md` problem tickets were exercised by this iter's activity. The iter touched P014 (open, reverse-trace update) only. Other open `.verifying.md` tickets (P021 etc.) were not exercised — RFC-002 work is in a different surface.

Prior-session README-cell drain (sub-step 9): not run this iter — no prior-session `yes — observed:` cells changed.

## Briefing Changes (Step 3)

Scanned this iter's signals against `docs/briefing/governance-workflow.md` + `docs/briefing/releases-and-ci.md`:

- **Added**: none — the "Multi-iter feature ship-signal" pattern is already canonical in CLAUDE.md §Commit Messages (codified `db9cf5d`); a duplicate in the briefing tree would add maintenance cost without signal gain. The "What's new" variant within shape (ii) is at n=2; not yet codify-worthy per Architect Note 1.
- **Removed**: none — no entries in this iter's context window were demonstrated stale.
- **Updated**: none.
- **README index refreshed**: no.

Tier 3 budget pass: deferred to next interactive retro (per-topic-file byte counts have not crossed `BRIEFING_TIER3_MAX_BYTES=5120` threshold based on the cheap-layer bucket aggregate of 29 KB across 4 topic files = ~7 KB each average; no MUST_SPLIT signal expected).

## Tickets Deferred

(none — Skill tool was available; ticketing was preferred via the Pipeline Instability section above per ADR-013 Rule 6 AFK fallback. No observations dropped without `cause: skill_unavailable`.)

## Codification Candidates (Step 4b)

| Kind | Shape | Suggested name / Target file | Scope / Flaw | Triggers / Evidence | Decision |
| ---- | ----- | ---------------------------- | ------------ | ------------------- | -------- |

(none new — both observed friction signals dedup against existing tickets P010 + P023; Step 2b Pipeline Instability handles evidence-append routing. No novel codification candidates surfaced this iter.)

## No Action Needed

- Architect Note 1's meta-ADR for "What's new" vs "Options table row" ship-signal shapes — already covered by CLAUDE.md "equivalent user-facing surface" language; n=2 evidence insufficient per inverse-P078 / P132 guard.
- The full RFC-002 chain (T1-T8) — landed cleanly across iters 1-9; the `## Verification` section in `RFC-002-...verifying.md` records the audit trail (full SHA chain T1 `34120cc` → T7 `450a355` → T8 `07311a1` + `d5ff125`).
