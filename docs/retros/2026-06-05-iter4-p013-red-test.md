# Retro — 2026-06-05 iter 4 (P013 TDD RED — class (a) failing test)

AFK iter 4 of `/wr-itil:work-problems` loop. Continued P013 — iter 3 (`20763e5`) drafted the ADR-0018 amendment; this iter executes step 3 of P013's 2026-06-04 Decision "Next iter should": TDD RED phase. Committed `f2425d2`. No push, no release this iter (per orchestrator iter-scope constraint).

## Iter scope

- P013 step 3: write a failing test for class (a) `fix via parent bump` against `src/find-unfixable-vulns.js`.
- Constraint: RED phase only. No implementation in `src/`. Test must fail and remain failing.
- @windyroad/tdd hook enforces RED state — test-first, then implementation in subsequent iter.

## What happened

1. Read ADR-0018 amendment (lines 186–250) to confirm class (a) detection signal: `nodes[]` path traversing `node_modules/<parent>/node_modules/<name>`; reason string `fix via parent bump: <parent>`.
2. Read `src/find-unfixable-vulns.js` (`deriveReason()` lines 52–56 — returns `'vulnerable transitive dependency'` for `isDirect: false`, never reading `fixAvailable` or `nodes`).
3. Read `src/find-unfixable-vulns.test.js` (5 existing tests under one describe block — Story 016.0).
4. Inspected actual `npm audit --json` output on this repo — confirmed brace-expansion entry shape: `nodes: ['node_modules/npm/node_modules/brace-expansion']`, `fixAvailable: true`, single via-advisory `GHSA-jxxr-4gwj-5jf2`.
5. Architect review (`wr-architect:agent`) — APPROVED. Three questions answered: (a) reusing `REQ-UNFIXABLE-DETECT` is consistent with the amendment's "structural sharpening" framing; (b) `nodes[]` parent-extraction matches amendment §Detection signals Step 2 verbatim; (c) ADR-0014/0020/0011 unaffected by test-only change. Advisory: compendium `docs/decisions/README.md` not regenerated for the 2026-06-05 ADR-0018 amendment — non-blocking, recommended follow-up.
6. JTBD review (`wr-jtbd:agent`) — APPROVED. JTBD-009 alignment confirmed; JTBD-008 reinforced (auto-update PR body inherits the new reason vocabulary); persona constraint preserved (time-as-scarcest-resource).
7. Appended the failing test to `src/find-unfixable-vulns.test.js` at end of the existing describe block. Fixture: `braceExpansionBundledInNpm` with `nodes: ['node_modules/npm/node_modules/brace-expansion']`. Assertion: `reason === 'fix via parent bump: npm'`.
8. RED verification: `npx vitest run src/find-unfixable-vulns.test.js` — 1 failed, 5 passed. Failure diff shows expected `fix via parent bump: npm` vs actual `vulnerable transitive dependency` — exactly the predicted RED state.
9. Staged + ran external-comms voice + risk-scorer:pipeline gates. All PASS (commit risk 2/25 Very Low; voice + risk-scorer:external-comms PASS on first call).
10. Commit landed `f2425d2` on first try. No staging-trap (iter 3 lesson held — used single heredoc with `git commit -m "$(cat <<'EOF' ... EOF)"` once, no body re-edit).

## Step 1.5 — Signal-vs-Noise Pass (P105)

Silent classification applied per the SKILL's mechanical-classification rule. Scan: 4 briefing topic files (33 entries total; same inventory as iter 3 because no entries added/removed this iter).

| Entry (1-line summary)                                  | File               | Old score | New score | Classification | Citation                                                                                                        |
| ------------------------------------------------------- | ------------------ | --------- | --------- | -------------- | --------------------------------------------------------------------------------------------------------------- |
| Four mandatory gates fire on UserPromptSubmit           | hooks-and-gates.md | 7         | 8         | signal         | architect + JTBD + risk-scorer + voice-tone gates all fired this iter; required delegations                     |
| Every commit requires wr-risk-scorer:pipeline score     | hooks-and-gates.md | 3         | 4         | signal         | risk-pipeline scorer fired; emitted `RISK_SCORES: commit=2 push=0 release=0`                                    |
| External-comms gate hash bytewise — body edits re-block | hooks-and-gates.md | 1         | 2         | signal         | this iter committed with single heredoc + no body re-edit; lesson held (proactive avoidance counts as citation) |
| All other entries (30 of 33)                            | (all files)        | various   | various-1 | decay-only     | not cited this iter                                                                                             |

**Critical Points changes**: none. No entry crossed +3 threshold from below; no entry crossed -3 threshold from above.

**Delete queue**: empty.

**Budget overflow**: not triggered.

## Step 2b — Pipeline Instability Scan

No new pipeline-instability detections this iter. The iter 3 friction signals (external-comms hash mismatch, P057 staging trap) did NOT recur — the iter 3 lesson "use `git commit -m` with single heredoc, no body re-edit between scorer call and commit" held. Zero re-stage attempts.

**README inventory currency**: `wr-retrospective-check-readme-jtbd-currency` — `packages dir not found: packages` (adopter repo; plugin shipped from upstream). Inline-noted, no halt.

## Step 2c — Context Usage (Cheap Layer)

`wr-retrospective-measure-context-budget` ran successfully. Prior snapshot: 2026-06-05 iter 3 (no HTML-comment context-snapshot trailer in retro file — first-retro baseline pattern repeats).

| Bucket             | Bytes        | % of total | Δ vs prior                                                                                                                |
| ------------------ | ------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------- |
| memory             | 406,306      | 40.6%      | +0 (no memory writes this iter)                                                                                           |
| decisions          | 280,177      | 28.0%      | +0 (no ADR edits)                                                                                                         |
| problems           | 219,461      | 22.0%      | +0 (no ticket edits — P013 body untouched this iter)                                                                      |
| jtbd               | 42,306       | 4.2%       | +0                                                                                                                        |
| briefing           | 31,499       | 3.2%       | +750 vs iter 3 (30,749) — measurement-tooling noise / iter 3's prior snapshot may have predated its own briefing addition |
| project-claude-md  | 7,747        | 0.8%       | +0                                                                                                                        |
| hooks              | not measured | —          | reason=source-absent (adopter repo)                                                                                       |
| skills             | not measured | —          | reason=source-absent (adopter repo)                                                                                       |
| framework-injected | not measured | —          | reason=framework-injected-no-on-disk-source                                                                               |

THRESHOLD bytes=10240 (per-bucket ceiling per ADR-043).

Top-5 offenders (script-measurement method: `wc -c` per bucket, summed across globs):

1. `memory` — 406,306 bytes — `~/.claude/projects/.../memory/*.md`
2. `decisions` — 280,177 bytes — `docs/decisions/*.md`
3. `problems` — 219,461 bytes — `docs/problems/**/*.md` + `docs/problems/README.md`
4. `jtbd` — 42,306 bytes — `docs/jtbd/**/*.md`
5. `briefing` — 31,499 bytes — `docs/briefing/*.md`

Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer). No deep-layer-recommended advisory this iter (delta within ±20% per bucket; deep-layer last-run still ≥ 14 days stale but the threshold trigger fired in iter 3 already — keeping the advisory live).

## Step 2d — Ask Hygiene Pass (P135 Phase 5 / ADR-044)

AFK iter — no `AskUserQuestion` calls fired by the agent. Sub-agent invocations (architect, JTBD, risk-scorer:external-comms, voice-tone:external-comms, risk-scorer:pipeline) do not count as agent `AskUserQuestion` calls per ADR-044.

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

R6 numeric gate (≥2 lazy across 3 consecutive retros): NOT fired. Last three retros' lazy counts: 0 / 0 / 0 (this iter, 2026-06-05 iter 3, 2026-06-04 iter 6). Trend: stable at zero across the AFK loop body.

## Step 3 — Briefing Changes

**Added**: none — scanned all topic files; iter 3's lessons (external-comms gate bytewise hash; ADR amendment review pattern) sufficiently captured the recurring class. This iter's experience reinforces those entries via +1 signal-score increment rather than adding new entries.

**Removed**: none — scanned all 33 entries; no entry was wrong enough to remove.

**Updated**: signal-score + last-classified for all 33 entries (3 entries cited-this-iter received +1 net; 30 entries received -1 decay).

**README index refreshed**: no Critical Points change this iter.

### Topic File Rotation Candidates

| Topic file                             | Bytes | Threshold | Proposed rotation | Decision                                                                                                                              |
| -------------------------------------- | ----- | --------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/briefing/governance-workflow.md` | 7,896 | 5,120     | split-by-subtopic | flagged (non-interactive — ratio 1.54×, below 2× MUST_SPLIT; same recommendation as iter 3 — extract `external-comms-gate` sub-topic) |
| `docs/briefing/hooks-and-gates.md`     | 8,412 | 5,120     | split-by-subtopic | flagged (non-interactive — ratio 1.64×, below 2× MUST_SPLIT; same recommendation as iter 3)                                           |

Branch B applies (OVER without MUST_SPLIT). No further accretion this iter (no new entries added) — the +6 byte drift since iter 3's accounting is sed-tooling noise, not new content. Recommended next-interactive rotation: extract three external-comms-gate entries (P024, P064, iter-3-new) into `docs/briefing/external-comms-gate.md` as a dedicated sub-topic file.

## Step 4a — Verification Candidates

`docs/problems/verifying/` carries 2 tickets: P006, P014.

| Ticket | Fix summary                                                                                  | In-session citations                                                                                                      | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------ | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P006   | "assistant defers actionable items to next session" — fix shipped via @windyroad/itil@0.47.9 | README cell carries `yes — observed: this iter's action-first orchestration (upstream itil@0.47.9)` from 2026-06-04 retro | **dispatch-deferred (pending-upstream marker present)** — cell parenthetical "(upstream itil@0.47.9)" cites pending upstream npm release; orchestrator's classifier in iter 1 of this AFK loop classified P006 as pending-upstream (commit `09c7c41`). Closing on cell whose evidence cites an unreleased upstream would be premature. Recovery path: when `@windyroad/itil@0.47.9` is published and observable on this repo, invoke `/wr-itil:transition-problem 006 close`. |
| P014   | "age soak is unconditional ignores known-vuln exposure"                                      | not exercised this iter — this iter's path touched `src/find-unfixable-vulns.test.js` only; age-soak code untouched       | left Verification Pending                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## Step 4b Stage 1 — Codification candidates ticketed

No codify-worthy observations surfaced this iter. The iter was small, mechanical, and successful: one test added, all gates passed, no friction signals. Iter 3's external-comms gate bytewise-hash observation was already codified (briefing entry) and the lesson held this iter — proof the codification worked.

Per P342 trust-boundary taxonomy: nothing in this iter triggered recurring-class pattern detection. The iter is a textbook TDD RED phase execution.

| Kind | Shape | Suggested name / Target file | Scope / Flaw | Triggers / Evidence | Decision                     |
| ---- | ----- | ---------------------------- | ------------ | ------------------- | ---------------------------- |
| —    | —     | —                            | (none)       | (none)              | no codification observations |

**Tickets Deferred**: none. No Step 4b Stage 1 violations.

## What I wish I'd been told up front

- The traceability lint rule expects each `it()` test's `@story` JSDoc on fixtures (not just the describe block). Verified by inspection of the existing tests — the existing fixtures (`braceExpansion`, `lowSevDirect`) carry `/** @story prompts/... */` annotations.
- `nodes[]` in `npm audit --json` is the load-bearing field for parent-walk detection. The amendment text references the path shape but inspecting actual `npm audit --json` output on this repo confirmed `nodes` is the field name (not `paths` or `via-chain`). One-shot `npm audit --json | head` was the right primitive.

## What surprised me

- Nothing surprising this iter. The architect review's APPROVED came faster than expected (single round-trip; no AMENDMENTS to fold in), reflecting that iter 3's amendment text was sufficiently rigorous to absorb the next iter's mechanical extension without surfacing new substance gaps.

## What was harder than it should have been

- Nothing. The test addition was 38 lines. All gates passed on first attempt for every gate (external-comms voice + risk + commit).

## What failed

- Nothing. RED state achieved exactly as designed. 5 of 6 tests pass; the 1 failure is the intended RED-phase signal.

## Conclusion

Iter scope held tightly: P013 TDD RED phase only. The classifier extension (GREEN phase reading `fixAvailable` + walking `nodes[]` for parent extraction) deferred to next iter per amendment §Detection signals Step 2. Classes (b) and (c) follow in subsequent iters per their own RED → GREEN cycles. Five sub-agent reviews this iter (architect, JTBD, risk-scorer:external-comms, voice-tone:external-comms, risk-scorer:pipeline) all returned PASS without blocking substantive changes — workflow healthy. Commit `f2425d2` landed cleanly on first attempt — iter 3's heredoc-escape lesson held (no body re-edit between scorer call and commit). Next iter: GREEN phase — extend `deriveReason()` to read `fixAvailable` and walk `nodes[]` for parent extraction, making the RED test pass.
