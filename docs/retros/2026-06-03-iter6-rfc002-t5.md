# Retro — 2026-06-03 iter 6 (RFC-002 T5 formatter rendering)

**Context**: AFK `/wr-itil:work-problems` iter 6. Orchestrator re-selected P014 / RFC-002 T5 per WSJF. Strict T5 scope: extend table / JSON / XML formatters with a per-row `viaExposureModifier` annotation surface for safe-update rows age-permitted only by the exposure-aware soak modifier. Default-OFF discipline preserved per JTBD-006.

## Outcome

- **Single chore commit landed**: `93343ee chore(formatters): render exposure-modifier findings in table/json/xml for RFC-002 T5`. 12 files (6 src + 6 paired test). 663 insertions / 20 deletions.
- **Full prepush clean**: lint:commits, lint (1 pre-existing warning unrelated), type-check, format:check, 361 unit + 4 e2e (365 total), lockfile, duplication, audit:ci.
- **TDD discipline preserved**: 6 RED → GREEN cycles, paired per ADR-0020 — output-utils, json-formatter, xml-formatter-utils, print-outdated-utils, print-outdated-handlers, print-outdated. No source edit without a paired failing test.
- **Architect / JTBD / voice-tone / risk-scorer gates all PASS**. Architect flagged 4 adjustments on the initial review (ADR-0014 mis-attribution → cite CLAUDE.md "Multi-iter feature ship-signal" + ADR-0019; ADR-0002 confirmation criterion 3 explicitly deferred to T6; positional 5-arg ceiling on `prepareJsonItems` accepted as the eslint `max-params: 5` boundary; XML child elements use camelCase per `<overridesHygiene>` precedent). All absorbed before any source edit.
- **Default-OFF discipline empirically verified**: 4 paired test cases assert byte-identical legacy output when `--exposure-aware-soak` is absent (per format).

## Iter scope vs delivered

| Item                                                                                   | Planned (iter brief) | Delivered   |
| -------------------------------------------------------------------------------------- | -------------------- | ----------- |
| Per-row `viaExposureModifier` annotation Map computed in `print-outdated.js`           | yes                  | yes         |
| `buildExposureMetaByPackage(auditData)` helper (severity + GHSA advisory ids)          | yes                  | yes         |
| `buildViaExposureModifierAnnotations({ safeRows, ..., prodMinAge, devMinAge })` helper | yes                  | yes         |
| `output-utils.js` `prepareJsonItems` 5th positional arg + per-item field               | yes                  | yes         |
| `json-formatter.js` object-row passthrough of `viaExposureModifier`                    | yes                  | yes         |
| `xml-formatter-utils.js` `<viaExposureModifier>` element with camelCase children       | yes                  | yes         |
| `print-outdated-utils.js` table star marker (`*`) + footnote dedup                     | yes                  | yes         |
| `print-outdated-handlers.js` JSDoc shape + thread-through                              | yes                  | yes         |
| `resolveExposureMaps` sibling helper extraction (line-cap relief, not original scope)  | NO (linter-driven)   | yes         |
| T6 docs (README + docs/api.md schema update)                                           | OUT                  | not touched |
| T7 live-case regression test under Critical-exposure fixture                           | OUT                  | not touched |

## Friction (in-iter)

1. **Governance-gate marker re-invalidation between sequential file edits**. The `wr-architect:agent` PASS marker required 2 re-invocations and `wr-jtbd:agent` required 3 across this iter — each fired the same agent on a marginal-delta plan, each returned PASS. Citations:
   - Architect re-confirm 1 (post output-utils.test.js edit attempt): block message _"jtbd policy file changed since last review. Re-run the jtbd agent"_ on attempted edit of `src/print-outdated-utils.test.js` after architect PASS was already on record.
   - Architect re-confirm 2 (post linter failure refactor): same block message on attempted edit of `src/print-outdated.js` for the `resolveExposureMaps` extraction.
   - JTBD re-confirms 1–3: identical block messages on `src/print-outdated-utils.test.js`, `src/print-outdated.js` (first attempt), `src/print-outdated.js` (refactor extraction).

   Observable cost: 5 extra agent invocations × ~30s each = ~2.5 min agent-time. Resolution: re-invoke the agent on each block; each returned PASS without surfacing new concerns. **Hypothesis** (not concrete log evidence — caveat per ADR-026 grounding): the marker key encodes a hash of touched files OR cross-policy-file siblings, and is invalidated when adjacent policy-file reads land between edits. Without specific log lines exhibiting the marker shape, this is borderline P342 — recording in Pipeline Instability section rather than auto-ticketing.

2. **`max-lines-per-function` cap on `printOutdated`**. Adding the T5 annotation-map computation pushed the function to 83 lines (cap 80). Resolved by extracting `resolveExposureMaps(options, auditData)` sibling helper — mirrors the existing `resolveUnfixable` / `resolveOverridesHygiene` / `fetchSharedAudit` extraction pattern explicitly cited in their JSDocs. Architect PASS re-confirmed the extraction. Routine; no new ticket pattern.

3. **External-comms gate marker key on Co-Authored-By trailer**. First risk-scorer review used a draft body WITHOUT the `Co-Authored-By:` line; gate denied the commit with a hash mismatch. Resolution: re-invoked `wr-risk-scorer:external-comms` with the EXACT verbatim body including trailers. Single retry sufficed. Already covered by **P023** (external-comms gate marker re-hashes on every draft-body delta); no new ticket needed.

## Decisions made silently (per ADR-044)

- Kept `prepareJsonItems` as 5 positional args (right at the eslint `max-params: 5` ceiling) rather than refactor to options-object now. Architect explicitly PASSed this choice with the note "any future addition forces an options-object refactor." Recording the constraint in the T5 commit body is sufficient for the next person to read.
- Used `<viaExposureModifier>` + camelCase child elements (`<severity>`, `<baseSoakDays>`, `<effectiveSoakDays>`, `<advisories><advisory>`) rather than the originally-proposed kebab-case children. Adopts the `<overridesHygiene>` precedent (RFC-001 T5 architect-correction at gate review) for JSON↔XML name-symmetry.
- Inlined `advisoryIdFromViaEntry` helper in `print-outdated.js` rather than refactoring out the duplicate of `src/find-unfixable-vulns.js`'s `advisoryId`. Keeps T5 scope strict; helper is 8 lines. Refactor can land as a follow-up problem ticket if the duplication becomes load-bearing.
- Skipped writing the `viaExposureModifier` field schema docs in README + docs/api.md (ADR-0002 Confirmation criterion 3). Explicitly deferred to T6 per the iter brief and the T5 commit body. The release-eligible `feat:` gate (per CLAUDE.md "Multi-iter feature ship-signal") still pends on T6 landing.

## Next iter candidate

RFC-002 **T6** — docs (README + `prompts/018.0-DEV-EXPOSURE-AWARE-SOAK.md` cross-references) explaining the opt-in default, the locked policy table, composition with the existing `--severity` filter, and the v2 graduation triggers. Closes ADR-0002 Confirmation criterion 3. Once T6 lands the README's Options table addition, the next commit can ship as `feat:` per the RFC-001 `355deee` pattern and trigger the semantic-release minor bump.

Or T7 — live-case regression test under Critical current-exposure fixture (asserts the 0-day floor applies with the flag set AND the unconditional 7-day soak blocks the candidate without the flag).
