# Iter 10 retro — P021 calculateAgeInDays injectable-clock extension

**Session**: 2026-06-02 AFK `/wr-itil:work-problems` iter 10
**Outcome**: P021 fix shipped (`dc64c44`) + lifecycle transition Open → Known Error (`b89957c`)

## What happened

Single backwards-compatible signature extension on `calculateAgeInDays(publishDate)` → `(publishDate, now = Date.now())`. RFC-001 T3's inline arithmetic in `src/overrides-hygiene.js` swapped back to the helper; `MS_PER_DAY` constant + local `computeAgeDays` helper + JSDoc divergence comment removed. Docs (`docs/api.md` + `docs/architecture.md`) synced one-line each. RED→GREEN cycle clean: new `[REQ-OVERRIDES-AGE]` test added in `src/age-calculator.test.js` failed (1248 vs 9) before the signature extension, passed after. Prepush clean (318 unit + 4 e2e). Two commits per ADR-014 single-grain: `dc64c44` fix, `b89957c` transition.

## Briefing Changes

- Added: none — pure ticket-execution iter, no new cross-session learnings observed.
- Removed: none.
- Updated: none.
- README index refreshed: no — Critical Points unchanged.

Scan evidence (per P332 anti-pattern guard): screened all 4 topic files' "What You Need to Know" + "What Will Surprise You" sections against iter 10's activity (single backwards-compat signature extension + ticket lifecycle). Zero candidate observations — the iter exercised already-documented patterns (TDD RED→GREEN, ADR-014 commit grain, P057 staging re-stage, external-comms gate re-fire on body delta, P010 commitlint subject-case). All five are already on existing tickets or briefing entries.

## Problems Created/Updated

- P021 — fix shipped (`dc64c44`); transitioned Open → Known Error (`b89957c`); 3 of 4 Investigation Tasks ticked. Remaining task ("Re-rate Priority and Effort at next /wr-itil:review-problems") stays unticked per its self-deferred wording.

## Verification Candidates

(None — P016 is the only `.verifying.md` ticket and was not exercised in iter 10's activity.)

## Pipeline Instability

| Signal                                                                                                                                                                                               | Category                  | Citations                                                                                                                                                                                                                            | Decision                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| External-comms gate marker re-hashed on commit-message subject reorder ("P021 known error" → "known error P021"), forcing a second pair of risk + voice-tone PASS verdicts on identical body content | Hook-protocol friction    | Iter 10 commit `b89957c`: first attempt blocked by commitlint subject-case (P010), reordered subject, gate re-fired demanding fresh `wr-risk-scorer:external-comms` + `wr-voice-tone:external-comms` verdicts despite unchanged body | recorded — matches carried-forward outstanding question #5 from iter 9 (re-hash on every draft-body delta). No new ticket — same pattern, same root cause; surfaces here as one more occurrence in the evidence trail.                                                                                                                                             |
| commitlint `subject-case` rule rejected `docs(problems): P021 known error …` (leading uppercase token); manual reorder to `docs(problems): known error P021 …` per established workaround            | Skill-contract violations | Iter 10 commit `b89957c` initial attempt blocked by `subject must not be sentence-case, start-case, pascal-case, upper-case [subject-case]`                                                                                          | matches existing ticket P010 (`manage-problem SKILL.md commit-message convention fails @commitlint/config-conventional subject-case rule`). New evidence: P010's hit count for ticket-lifecycle transition commits where the documented subject `docs(problems): P<NNN> <status> — <summary>` collides with commitlint is now ≥3 across iter 7 / iter 8 / iter 10. |

## Topic File Rotation Candidates

(`check-briefing-budgets.sh` not invoked this iter — briefing bucket measured at 29265 bytes total across 4 topic files; per-file threshold 5120 bytes default not breached at aggregate level. Per-file rotation pass deferred to a non-AFK retro.)

## Ask Hygiene

See `docs/retros/2026-06-02-iter10-ask-hygiene.md` — **Lazy count: 0** (zero AskUserQuestion calls fired this iter; AFK mode per P135 / ADR-044 mid-loop ban).

## Context Usage (Cheap Layer)

| Bucket            | Bytes  | % of total | Δ vs prior                               |
| ----------------- | ------ | ---------- | ---------------------------------------- |
| memory            | 386571 | 42%        | not measured — prior snapshot not parsed |
| decisions         | 270092 | 30%        | not measured                             |
| problems          | 166363 | 18%        | not measured                             |
| jtbd              | 41172  | 5%         | not measured                             |
| briefing          | 29265  | 3%         | not measured                             |
| project-claude-md | 6243   | 1%         | not measured                             |
| hooks             | 0      | 0%         | not measured                             |
| skills            | 0      | 0%         | not measured                             |

Threshold: 10240 bytes per bucket. `decisions` + `problems` + `memory` exceed threshold; deep-layer analysis available via `/wr-retrospective:analyze-context`.

## Codification Candidates

(None — iter was straightforward ticket execution with established patterns. Carried-forward outstanding questions #5 + #6 from iter 9 are codification surfaces but already named in prior retros; no new shapes surfaced this iter.)

## No Action Needed

- Architect + JTBD reviews both PASS — no ADR-shaped decision needed; signature extension is backwards-compatible with no persona-job impact.
- `wr-risk-scorer:pipeline` returned commit residual 4/25 (Low) for fix, 2/25 (Very Low) for transition — both within appetite.
