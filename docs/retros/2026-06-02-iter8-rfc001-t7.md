# Iter 8 Retrospective — RFC-001 T7

**Date**: 2026-06-02
**Session shape**: AFK `/wr-itil:work-problems` iter 8 — RFC-001 T7 (live-case regression test) + cross-session README reconcile drain (P020/P021/P022).
**Commits**: `e8fcb6b` (chore reconcile README) → `42031d5` (test RFC-001 T7).
**Result**: T7 GREEN (3/3 new it-blocks); P013 advanced from T1-T6+T8 → T1-T8 done; T9 (in-progress → verifying transition + Verification draft) remaining for next iter / orchestrator dispatch.

## Briefing Changes

- Added: none — no new observations surfaced this iter that meet ADR-044 framework-resolution boundary for briefing-tier durability. Scan evidence: per-section scan against `docs/briefing/{hooks-and-gates.md, governance-workflow.md, releases-and-ci.md, autonomous-dep-updates.md}` — 4 candidate observations screened (heredoc backtick escape gotcha, README reconcile preflight working as designed, external-comms re-fire after marker-hash drift, manage-problem auto-route from Step 0 preflight), all classified as either one-shot or already-documented.
- Removed: none.
- Updated: none.
- README index refreshed: no Critical Points change.

## Signal-vs-Noise Pass (P105)

Lean-mode silent decay only — no new cites accumulated this iter beyond entries already at saturation scores (the `gates fire every prompt` entry remains the dominant signal). Decay applied silently to all entries; no rotation prompts triggered (no entry breached the +3 promote or -3 delete threshold). Per-entry HTML-comment timestamps preserved; no `last-classified` field rewrites this iter to keep the retro lean per P147.

## Verification Candidates

(none — no `.verifying.md` ticket was exercised in this session; P016 remains `no — not observed`; same-session exclusion applies to nothing because no fix shipped this iter.)

## Pipeline Instability

- **README reconcile preflight detected committed cross-session drift on entry to manage-problem** — Step 0 preflight script reported 3 MISSING WSJF Rankings rows (P020 / P021 / P022), classifier returned HALT_ROUTE_RECONCILE; AFK auto-route to `/wr-itil:reconcile-readme` ran cleanly. **Category**: Skill-contract violations (existing P011 manifestation — P165 PreCommit refresh hook overrides capture-problem's deferred-README contract). **Citations**: `wr-itil-reconcile-readme docs/problems` (turn 3 — `MISSING P020 wsjf-rankings: actual=open`, `MISSING P021 ...`, `MISSING P022 ...`); `wr-itil-classify-readme-drift` returned `HALT_ROUTE_RECONCILE uncovered=3`. **Decision**: P011 already covers this exact regression class (Known Error WSJF 6.0). No new ticket. The Step 0 preflight + reconcile-readme auto-route worked as designed — exactly P118's contract.

## Topic File Rotation Candidates

The `check-briefing-budgets.sh` script reported two OVER lines (no MUST_SPLIT — both files at ratio < 2×). Per P247 anti-pattern enforcement, rotation IS required; per AFK Rule 6 and the table's documented `flagged (non-interactive)` semantics, surfacing here for user review on return.

| Topic file                             | Bytes | Threshold | Proposed rotation                                                                                     | Decision                  |
| -------------------------------------- | ----- | --------- | ----------------------------------------------------------------------------------------------------- | ------------------------- |
| `docs/briefing/governance-workflow.md` | 7891  | 5120      | split-by-date (archive entries with `first-written` ≤ 2026-05-13 to `governance-workflow-archive.md`) | flagged (non-interactive) |
| `docs/briefing/hooks-and-gates.md`     | 6184  | 5120      | split-by-date (archive entries with `first-written` ≤ 2026-05-13 to `hooks-and-gates-archive.md`)     | flagged (non-interactive) |

## Ask Hygiene (P135 Phase 5 / ADR-044)

(no `AskUserQuestion` calls fired this iter — trail at `docs/retros/2026-06-02-iter8-ask-hygiene.md`)

- **Lazy count: 0** (target met)
- **Direction count: 0**
- **Deviation-approval count: 0**
- **Override count: 0**
- **Silent-framework count: 0**
- **Taste count: 0**
- **Correction-followup count: 0**

## Codification Candidates

(none — no recurring class-of-behaviour observation surfaced this iter; all observations scanned were either one-shot mechanical work or already-ticketed regressions.)

## No Action Needed

- T7 architect verdict PASS — no new ADR required; ADR-0020 cross-module-integration carve-out for `test/cli.*.test.js` placement is the load-bearing existing decision.
- T7 JTBD verdict PASS-WITH-NOTES — no new citations beyond RFC-001's existing descriptive-cite set (JTBD-001 / JTBD-204 / JTBD-006). `JTBD-203` (Reproducible audit artefacts) optional descriptive cite acknowledged but not load-bearing.
- README reconcile drift (P020/P021/P022 staging gap) is the latest manifestation of P011 (Known Error WSJF 6.0 — P165 PreCommit refresh hook overrides capture-problem's deferred-README contract). P118 + reconcile-as-preflight robustness layer is the recovery contract; both fired correctly.
- External-comms / voice-tone gate re-fired after my first T7 commit attempt hit a marker-hash mismatch (literal backslashes from heredoc backtick escape). Recovered cleanly via unescaped-backticks retry. One-shot shell quoting gotcha; not codifiable beyond memory-tier "always use single-quoted heredoc for commit-msg bodies and never escape backticks". Already implicit in the working-shape established this session.

## Context Usage (Cheap Layer)

| Bucket             | Bytes          | Note                                                 |
| ------------------ | -------------- | ---------------------------------------------------- |
| memory             | 386571         | (no prior snapshot — first measurement this project) |
| decisions          | 270092         | (no prior snapshot — first measurement this project) |
| problems           | 166074         | (no prior snapshot — first measurement this project) |
| jtbd               | 41172          | (no prior snapshot — first measurement this project) |
| briefing           | 29265          | (no prior snapshot — first measurement this project) |
| project-claude-md  | 6243           | (no prior snapshot — first measurement this project) |
| hooks              | 0              | not measured — adopter-tree path may differ          |
| skills             | 0              | not measured — adopter-tree path may differ          |
| framework-injected | (not measured) | reason: framework-injected-no-on-disk-source         |

THRESHOLD: 10240 bytes (cheap-layer report ceiling — not breached).

Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer).
