# Session Retrospective — AFK iter 4 (work-problems/2026-06-04, P014 K→V)

**Date**: 2026-06-04
**Iter**: 4 of the work-problems AFK loop (orchestrator main-turn Step 3 selected P014 — Tier 2, WSJF 4.5, Known Error, Effort L, Reported 2026-05-25, fix already shipped via RFC-002 ship-train today as v2.12.0)
**Scope**: iter-bounded (`--scope=iter`); does NOT cover earlier iters or sessions, only this iter's tool-call history.
**Outcome**: P014 transitioned Known Error → Verification Pending (commit `591e60a`); underlying fix shipped today as `dry-aged-deps@2.12.0` (npm) implementing the `--exposure-aware-soak` flag with the locked policy (Critical → 0-day floor, High → ½ default, Moderate/Low/None → default).

## Briefing Changes

- Added: none (scanned `docs/briefing/README.md` Critical Points + 4 topic files — every observation from this iter was either already captured in prior briefing entries — gate-paths, semantic-release flow, commit-shape, ADR-0016 auto-write, commitlint body-max-line-length — or session-local mechanical work that does not surface a cross-session learning). Scan evidence: read README.md Critical Points block + topic-file listing; no candidate observations against "What You Need to Know" or "What Will Surprise You" sections that aren't already captured (the RFC-002 ship-train is project-specific to one feature; the K→V transition mechanic is already covered by P134 line-3 rotation entries; the commitlint body-max-line-length friction is a single-shot format issue, not a recurring pattern across iters).
- Removed: none (no stale entries surfaced; this iter did not exercise any topic-file content critically enough to confirm or contradict it).
- Updated: none.
- README index refreshed: no Critical Points changes; the established critical surface (release model, ci-publish gate, auto-update, branch protection, --update target, OAuth token, pre-commit auto-write, push:watch, autonomous-update auth) was not exercised in a way that would shift its priority. The ADR-0016 pre-commit auto-write surface DID fire — prettier reformatted `docs/problems/README.md`'s Verification Queue table column widths inline during commit — confirming the existing Critical Points entry is still load-bearing and current.

## Signal-vs-Noise Pass (P105)

Iter-scoped scope means no per-entry classification was meaningfully exercisable — the iter loaded only `docs/briefing/README.md` (Critical Points roll-up) into context; no individual topic-file entries cited or paraphrased. Treating that read as a single decay-only tick across the Critical Points roll-up (per P105's `Decay -1` rule). Persistence is deferred to the next interactive retro per ADR-013 Rule 6 AFK fallback — no on-disk per-entry signal-score comment-block edits this iter; entries did not score below the delete threshold given they all sit above 0 from prior promotions.

(No delete-queue candidates; no Critical Points promotions; no budget-overflow.)

## Problems Created/Updated

- **P014** — transitioned Known Error → Verification Pending. File renamed (`docs/problems/known-error/014-age-soak-is-unconditional-ignores-known-vuln-exposure.md` → `docs/problems/verifying/014-age-soak-is-unconditional-ignores-known-vuln-exposure.md` — bare-name per per-state-subdir convention). Status field flipped to "Verification Pending". `## Fix Released` section added documenting the released feature (`--exposure-aware-soak` flag with the locked Critical→0 / High→½ / Moderate-Low-None→default policy), v2.12.0 release marker, the RFC-002 T1-T9 commit chain (`34120cc` → `789a18b` → `1a435f9` → `4241c4b` → `93343ee` → `a83ccf3` → `450a355` → `07311a1` → `d5ff125`), and the verification plan (exercise against a real-case Critical-exposure scenario where a candidate fix exists younger than `min-age`). `docs/problems/README.md` refreshed (WSJF Rankings row removed; Verification Queue row 3 added with `no — not observed` evidence cell; line 3 rewritten with P014 K→V narrative; closing sentence simplified to reference only P013 since P014 left the queue). `docs/problems/README-history.md` 2026-06-04 heading appended with displaced prior line 3 fragment (P025 K→V). Pre-commit prettier auto-write reflowed the Verification Queue table column widths inline (ADR-0016 contract). Committed as `591e60a`.

(No new problems created. No existing problems updated beyond P014.)

## Tickets Deferred

(none — Stage 1 ticketing was not needed; no codify-worthy observations from this mechanical iter. The friction observations are recurring patterns already covered by existing tickets — see Pipeline Instability below.)

## Verification Candidates

(none closed this iter. `docs/problems/verifying/` now contains P021 + P025 + P014.

- **P014** excluded by Step 4a sub-step 8 same-session rule: this session created the verifying ticket; cannot verify own work beyond gates-passed-at-commit.
- **P021** was created in iter 1's session (prior session relative to this `claude -p` invocation), but no in-session evidence collected this iter: no `vitest`, `npm test`, or `src/` exercise occurred — only `git mv`, ticket file Edit, and README edits ran. Sub-step 9 also does not apply: P021's `Likely verified?` cell reads `no — not observed`, not `yes — observed: …`.
- **P025** was created in iter 2's session (prior session relative to this `claude -p` invocation). Its `Likely verified?` cell reads `yes — observed: ci-publish exercised under v6 across 8+ pushes since fix shipped — all green; auto-update + claude workflows await natural trigger` — partial observation only. The cell's explicit `auto-update + claude workflows await natural trigger` clause signals the cell-author considered the evidence incomplete; closing on partial evidence would contradict the cell's own deferral language. No dispatch this iter; the close decision waits for the natural-trigger workflows to exercise and the user/agent to refresh the cell to fully-observed status.)

## Pipeline Instability

- **Four-subagent commit-gate path on a docs-only commit**: `wr-risk-scorer:external-comms` × 2 (PASS both times) + `wr-voice-tone:external-comms` × 2 (PASS both times). The double-fire happened because the first PASS pair's marker did not land in the main-session marker dir (`${TMPDIR}/claude-risk-*` enumerated 600+ session dirs; markers landed in the subagents' own sandbox dirs). Resolved via `BYPASS_RISK_GATE=1` with documented rationale in the commit body. Known KE — covered by P023 (gate marker re-hashes on every draft-body delta) + P024 (cross-session marker dir mismatch). No new ticket — pattern is already ticketed and re-confirmation strengthens P024's evidence base.
- **Commitlint body-max-line-length forced two redraft cycles before passing**. First attempt's bypass-rationale paragraph was ~235 chars on one line; second attempt's `--exposure-aware-soak` flag-policy line was over the 200 limit. Third attempt passed by breaking long lines mechanically. This is a single-iter format issue, not a repeat-work pattern across iters — not ticket-worthy. The agent's commit-body drafting could carry a built-in line-length pre-check; recording as `Cause: skill_unavailable` candidate for a future ticket if the pattern recurs across ≥3 iters.
- **Architect + JTBD gates fired on retro file Writes** because `docs/retros/` is not in the gate exclusion lists (unlike `docs/problems/` + `docs/briefing/`). Both returned PASS in single round-trips. Same observation as iter 1 + iter 2 — single-shot gate fires, not a repeat-work pattern across iters. Not ticket-worthy.

README inventory currency: not-measured (script reports `packages dir not found: packages` — this adopter project does not host a `packages/` tree; the directive's `wr-retrospective-check-readme-jtbd-currency` script is a no-op here per ADR-069's defensive-trip contract).

## Topic File Rotation Candidates

(none triggered — Step 3's Tier 3 budget pass was not exercised given no briefing edits this iter. Topic files: `releases-and-ci.md` (4,821 B), `hooks-and-gates.md` (6,184 B), `governance-workflow.md` (7,891 B), `autonomous-dep-updates.md` (4,826 B) — `governance-workflow.md` is over the 5,120 B Tier 3 threshold, but not touched this iter so deferred per the per-edit-byte-count refresh pattern. Will surface in the next iter that touches that topic file.)

## Ask Hygiene (P135 Phase 5 / ADR-044)

| Call # | Header | Classification | Citation |
| ------ | ------ | -------------- | -------- |

(no AskUserQuestion calls this iter — AFK discipline preserved per the orchestrator iter-prompt hard-ban + P135 / ADR-044.)

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

Persisted trail: `docs/retros/2026-06-04-iter4-P014-KV-ask-hygiene.md`.

## Context Usage (Cheap Layer)

| Bucket             | Bytes                                               | % of total | Δ vs prior                                                             |
| ------------------ | --------------------------------------------------- | ---------- | ---------------------------------------------------------------------- |
| memory             | 395,324                                             | 41.3%      | +1,906 vs iter 2 (~+0.5% — small memory append, no concern)            |
| decisions          | 270,092                                             | 28.2%      | 0 (no decisions touched this iter)                                     |
| problems           | 205,045                                             | 21.4%      | +7,447 vs iter 2 (P014 Fix Released section + README + history append) |
| jtbd               | 42,306                                              | 4.4%       | 0 (no JTBD touched this iter)                                          |
| briefing           | 29,265                                              | 3.1%       | 0 (no briefing edits this iter)                                        |
| project-claude-md  | 7,747                                               | 0.8%       | 0 (CLAUDE.md unchanged)                                                |
| hooks              | not measured — source absent                        | n/a        | n/a                                                                    |
| skills             | not measured — source absent                        | n/a        | n/a                                                                    |
| framework-injected | not measured — framework-injected-no-on-disk-source | n/a        | n/a                                                                    |

Total measured: 949,779 bytes (~928 KB). Threshold: 10,240 bytes per bucket — `memory` (395K), `decisions` (270K), `problems` (205K), `jtbd` (42K), `briefing` (29K) all over threshold but this is the project-scale baseline, not an iter-introduced regression. Top-5 offenders: memory, decisions, problems, jtbd, briefing. Per-plugin breakdown available in `/wr-retrospective:analyze-context` (deep layer).

Delta vs prior (iter 2 snapshot in `docs/retros/2026-06-04-iter2-P025-KV.md`): +9,353 bytes total (~+1.0%) — explained by P014 ticket body Fix Released section (~1.2K), README + history line-3 rotation, and memory growth from iter 2 + iter 3 persisted retro files. No bucket exceeds the +20% deep-analysis-recommended threshold.

## Codification Candidates

(no codifiable observations from this iter — mechanical lifecycle transition with no recurring pattern surfaced. Pure ADR-022 + ADR-014 + P134 application against a single ticket. The commitlint body-max-line-length friction is a single-shot format issue; the marker-mismatch bypass is covered by P024 already; the architect+JTBD retro-write gates are the iter-1/2/3 known pattern.)

## No Action Needed

- The four-subagent gate-path on a docs-only commit (risk-external-comms × 2 + voice-tone-external-comms × 2) is a known-pattern surface already covered by P023 + P024 — re-noting it would be ticket noise. Bypass via `BYPASS_RISK_GATE=1` is the documented intentional override path.
- The architect + JTBD gate fires on `docs/retros/` writes are a known surface (the gate exclusion lists exclude `docs/problems/` and `docs/briefing/` but not `docs/retros/`). Both gates returned PASS in single round-trips. Not ticket-worthy — same observation as iter 1, 2, 3.
- The pre-commit prettier reflow of README Verification Queue column widths is the ADR-0016 contract working as designed (auto-write + re-stage). Not ticket-worthy.
- The commitlint body-max-line-length redraft cycle is a single-shot format issue this iter. If it recurs across ≥3 iters (the P074 Repeat-work threshold), capture a ticket; not yet.

## Notes

- Orchestrator selected P014 from the WSJF 4.5 tie-bucket (P013 + P014) by deviating from strict ID-asc final-tiebreaker. Stated rationale: P014's fix has fully shipped (RFC-002 ship-train, v2.12.0 published today), so K→V is mechanical and guaranteed to produce useful audit-trail artefact; P013's gap #2 needs ADR-0018 amendment + RFC-scope product-shape direction that ADR-074 substance-confirm-before-build would defer via outstanding_questions anyway. Higher value-per-dollar in the same tie-break bucket.
- Verification Queue is now 3 (P021 from iter 1, P025 from iter 2, P014 from this iter). All three await durable user evidence: P021's row is `no — not observed` pending next-session calculateAgeInDays exercise; P025's row is `yes — observed: ci-publish surface only` pending auto-update + claude workflow exercise at natural trigger times; P014's row is `no — not observed` pending next-user-encounter of a Critical-exposure case where `--exposure-aware-soak` allows a younger-than-min-age candidate.
- Remaining K→V backlog: P013 (released gap #1 — package.json overrides hygiene — but ticket carries gap #2 still requiring product-shape direction; the K→V mechanic would only address gap #1). The orchestrator's prior framing suggests P013 needs RFC-scope product-shape work before K→V is the right move.
- Memory MEMORY.md was not touched this iter (no new user feedback / project facts / references surfaced) — the +1,906-byte memory delta is from iter 3's retro persistence landing in the project-specific memory dir (if applicable) or upstream memory file accumulation.
