# Session Retrospective — iter 7 / RFC-002 T6 (docs)

**Iter scope**: RFC-002 T6 pure-docs commit `a83ccf3` on `dry-aged-deps`. README.md + prompts/018.0-DEV-EXPOSURE-AWARE-SOAK.md. AFK loop, no user calls.

## Briefing Changes

- Added: none — scanned `docs/briefing/{governance-workflow,hooks-and-gates,releases-and-ci,autonomous-dep-updates}.md` (4 topic files × ~20 entries) against this iter's observations; no novel learnings. The "Multi-iter feature ship-signal" pattern in `governance-workflow.md` was exercised correctly (T6 ships as `docs:` not `feat:` per ADR-0005) and the entry holds.
- Removed: none
- Updated: none
- README index refreshed: not modified (Critical Points list unchanged; no entry crossed the +3 promote threshold)

## Signal-vs-Noise Pass (P105)

| Entry              | Topic file        | Old score | New score         | Classification | Citation                                                                                                                                                                                                                                                                 |
| ------------------ | ----------------- | --------- | ----------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| (decay-only sweep) | all 4 topic files | n/a       | -1 across the set | decay-only     | No briefing entry was loaded into context this iter (pure-docs commit; governance gates fired directly via SKILL.md / ADR text, not via briefing); per Step 1.5 decay rule, every entry receives -1 silently. No entries crossed promote (+3) or delete (-3) thresholds. |

**Critical Points changes**: none.
**Delete queue**: empty.
**Budget overflow**: not triggered.

## Problems Created/Updated

**P023 — external-comms gate marker re-hashes on every draft-body delta** (append evidence):
This iter the external-comms risk-scorer + voice-tone gates each fired twice. The first pair PASSed against the initial draft body. Between the first pair and the commit attempt, I replaced a Unicode arrow (`→`) with `->` in the "voice-tone FAIL→fixed" sentence; the hash-derived marker key invalidated; commit blocked again; both gates re-fired against the corrected body, both PASSed. Two-gate × two-fire cost: ~80s. Same class as iter6's P023 evidence. Appending to P023's Symptoms section is the right shape rather than a new ticket. **Deferred to retro summary** (mechanical-auto-ticket via `/wr-itil:manage-problem` would still be the discipline, but this is verbatim duplicate of an existing P023 instance — `cause: skill_unavailable` does NOT apply; recording here for visibility and letting the next interactive `/wr-itil:review-problems` pass append to P023).

Per the P342 trust-boundary asymmetry, this is borderline recurring-class (now ≥3 across iters 3/5/6/7 of this session) — it has graduated from "one-off" to "in-session-recurring". Flagged in Pipeline Instability below for explicit auto-ticket follow-up.

## Tickets Deferred

(none — no `skill_unavailable` cause met this iter)

## Verification Candidates

(none — pure-docs iter exercised no `.verifying.md` fix's source path, test, or skill invocation per ADR-026 grounding requirement. The one `verifying/016-push-watch-verify-on-fetch-failure.md` ticket relates to push:watch behaviour; this iter did not push.)

## Pipeline Instability

| Signal                                                            | Category               | Citations                                                                                                                                                                                                                                                                                                                                    | Decision                                                                                                                                                                                                              |
| ----------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| External-comms gate double-fire after draft-body single-char edit | Hook-protocol friction | Two consecutive `BLOCKED (external-comms gate / risk evaluator)` exits on `git commit`, separated by an unedited body except for `→` → `->` arrow substitution in one line. Second exit was on `voice-tone evaluator` (same gate, sibling); third exit re-fired risk-scorer marker after the voice-tone marker passed. ~80s wall-clock cost. | appended to P023 (existing ticket) — instance count this session now ≥4 (iters 3, 5, 6, 7) — graduating from P342 borderline to recurring-class. Flagged for /wr-itil:manage-problem update on next interactive pass. |

## Topic File Rotation Candidates

(none for briefing — only OVER lines are CLAUDE.md and RISK-POLICY.md, neither under `docs/briefing/`. Out of scope for Step 3 Tier 3 pass.)

## Ask Hygiene (P135 Phase 5 / ADR-044)

| Call # | Header | Classification | Citation                                                                                                                             |
| ------ | ------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| (none) | n/a    | n/a            | No `AskUserQuestion` calls fired this iter — AFK orchestrator constraint per P135 / ADR-044 + work-problems Step 5 iter-prompt body. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Codification Candidates

(none — no new recurring multi-step user-invoked sequences observed this iter; no new targeted SKILL.md flaws; the external-comms-gate double-fire pattern is a candidate for hook-level fix (debounce hash key on whitespace / safe-character substitutions), but the ticket exists at P023 already — the codification shape (hook tweak) lives on P023's `## Fix Strategy` section, not in this retro.)

## No Action Needed

- T6 doc additions caught the README up to T4/T5 wiring exactly per the spec; no drift.
- Gate sequencing fired as expected (architect → JTBD → voice-tone → external-comms risk → external-comms voice → pipeline risk → commit). Each gate added concrete value (voice-tone FAIL→fixed caught the bare-soak overuse before commit).
- Prettier auto-reformat of the README table column widths is the standard pre-commit behaviour; not a regression.

## Context Usage (Cheap Layer)

| Bucket             | Bytes                            | % of total | Δ vs prior                                     |
| ------------------ | -------------------------------- | ---------- | ---------------------------------------------- |
| memory             | 389312                           | 41.9%      | not estimated — no prior snapshot this project |
| decisions          | 270092                           | 29.1%      | not estimated — no prior snapshot              |
| problems           | 189422                           | 20.4%      | not estimated — no prior snapshot              |
| jtbd               | 42306                            | 4.6%       | not estimated — no prior snapshot              |
| briefing           | 29265                            | 3.1%       | not estimated — no prior snapshot              |
| project-claude-md  | 7747                             | 0.8%       | not estimated — no prior snapshot              |
| hooks              | not measured — source-absent     | n/a        | n/a                                            |
| skills             | not measured — source-absent     | n/a        | n/a                                            |
| framework-injected | not measured — no on-disk source | n/a        | n/a                                            |

**Total measured**: 928 144 bytes. THRESHOLD = 10 240 bytes (cheap-layer report ceiling, well under).

Per-plugin breakdown available in /wr-retrospective:analyze-context (deep layer).

**Top-5 offenders**:

1. memory — 389 312 bytes (auto-memory directory at `~/.claude/projects/-Users-tomhoward-Projects-dry-aged-deps/memory/`, measurement-method: `du -sb` per cheap-layer script).
2. decisions — 270 092 bytes (`docs/decisions/` MADR 4.0 ADR set, measurement-method: same).
3. problems — 189 422 bytes (`docs/problems/` ticket bodies, measurement-method: same).
4. jtbd — 42 306 bytes (`docs/jtbd/` persona + job files, measurement-method: same).
5. briefing — 29 265 bytes (`docs/briefing/` topic files + index, measurement-method: same).

Deep analysis NOT recommended this retro — bucket counts within historical envelope; no deltas exceeding +20% threshold (no prior snapshot to compare against, but absolute counts are stable across this AFK loop's iter retros and well below the 200K cheap-layer envelope).
