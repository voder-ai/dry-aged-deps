# Session Retrospective — iter 8 / RFC-002 T7 (test)

**Iter scope**: RFC-002 T7 live-case regression test commit `450a355` on `dry-aged-deps`. New file `test/cli.exposure-aware-soak-regression.test.js` (137 insertions). AFK loop, no user calls.

## Briefing Changes

- Added: none — scanned `docs/briefing/{governance-workflow,hooks-and-gates,releases-and-ci,autonomous-dep-updates}.md` (4 topic files × ~20 entries) against this iter's observations; no novel learnings. The "Multi-iter feature ship-signal" pattern in `governance-workflow.md` was exercised correctly again (T7 ships as `test:` not `feat:` per ADR-0005 + CLAUDE.md ship-signal codification) — entry holds.
- Removed: none
- Updated: none
- README index refreshed: not modified (Critical Points list unchanged; no entry crossed the +3 promote threshold)

## Signal-vs-Noise Pass (P105)

| Entry              | Topic file        | Old score | New score         | Classification | Citation                                                                                                                                                                                                                                                                                                  |
| ------------------ | ----------------- | --------- | ----------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (decay-only sweep) | all 4 topic files | n/a       | -1 across the set | decay-only     | No briefing entry was loaded into context this iter (test-only commit; governance gates fired directly via SKILL.md / ADR text + 018.0 spec + RFC-002 RFC body — not via briefing); per Step 1.5 decay rule, every entry receives -1 silently. No entries crossed promote (+3) or delete (-3) thresholds. |

**Critical Points changes**: none.
**Delete queue**: empty.
**Budget overflow**: not triggered.

## Problems Created/Updated

(none created this iter — see Pipeline Instability below for the recurring external-comms gate friction that continues to graduate.)

## Tickets Deferred

(none — no `skill_unavailable` cause met this iter)

## Verification Candidates

(none — test-only iter exercised no `.verifying.md` fix's source path, test, or skill invocation per ADR-026 grounding requirement. The one `verifying/016-push-watch-verify-on-fetch-failure.md` ticket relates to `push:watch` behaviour; this iter did not push.)

## Pipeline Instability

| Signal                                                                                                                  | Category               | Citations                                                                                                                                                                                                                                                                                          | Decision                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| External-comms gate fires unconditionally before every `git commit` even when commit-body is risk-trivial               | Hook-protocol friction | First `git commit` blocked with `BLOCKED (external-comms gate / risk evaluator)` — must run `wr-risk-scorer:external-comms` + `wr-voice-tone:external-comms` to clear (both PASS first-try on the test-only commit). Cost ~30s wall-clock for a commit with zero external-comms surface.           | appended to P023 (existing ticket — instance count this session ≥5: iters 3, 5, 6, 7, 8). The class is now stably recurring across both `docs:` and `test:` commit types — not just `docs:`/`feat:`/`fix:`. Flagged for `/wr-itil:manage-problem` update on next interactive pass.                                             |
| Pipeline risk-scorer gate fires AFTER external-comms PASS — second commit-block per session subprocess                  | Hook-protocol friction | After external-comms gates cleared, second `git commit` blocked with `Commit blocked: No commit risk score found. Delegate to wr-risk-scorer:pipeline...`. Required separate `wr-risk-scorer:pipeline` invocation (verdict: commit=2/25 Very Low — risk-neutral). Cost ~30s additional wall-clock. | new signal class — `pipeline-risk-scorer-required-per-subprocess`. Not covered by P023 (P023 is external-comms specific). Recurring-class observation per the P342 trust-boundary taxonomy — auto-ticket candidate. Flagged below in Codification Candidates as a new problem ticket for `/wr-itil:capture-problem` follow-up. |
| Staging area lost between blocked-commit attempts — `nothing added to commit but untracked files present` after re-edit | Repeat-work friction   | After first blocked commit (external-comms gate), the staged file fell back to untracked. Second `git commit` invocation showed "nothing added to commit". Required re-run of `git add test/cli.exposure-aware-soak-regression.test.js` before the third (successful) commit attempt.              | one-off observation this iter — possibly a hook-side-effect that resets the index when the commit-message hook fails. Not yet recurring (n=1 this session). Leave under observation; if it fires again in subsequent iters, capture as a sibling-ticket to P057 (P057 covers post-`git mv` re-stage friction).                 |

## Topic File Rotation Candidates

(none for briefing — `check-briefing-budgets.sh` reports OVER lines only for `CLAUDE.md` (7747 bytes) and `RISK-POLICY.md` (9766 bytes), neither under `docs/briefing/`. Out of scope for Step 3 Tier 3 pass.)

## Ask Hygiene (P135 Phase 5 / ADR-044)

| Call # | Header | Classification | Citation                                                                                                                             |
| ------ | ------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| (none) | n/a    | n/a            | No `AskUserQuestion` calls fired this iter — AFK orchestrator constraint per P135 / ADR-044 + work-problems Step 5 iter-prompt body. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Codification Candidates

| Kind    | Shape | Suggested name / Target file                                                                                                             | Scope / Flaw                                                                                                                                                                                                                                                                                                               | Triggers / Evidence                                                                                                                                                                                                                                                                                                 | Decision                                                                                                                                                                                                                                                      |
| ------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| create  | hook  | `pipeline-risk-scorer-auto-pass-on-risk-neutral-test-only-commits.sh` (or settings entry to skip the gate when staged diff is test-only) | Pipeline risk-scorer fires per-subprocess even when staged diff is test-only with zero src/ changes. The scorer's own verdict was `commit=2/25 Very Low (risk-neutral)`; the gate could pre-compute this for the test-only-no-deps-no-CI case and emit the marker without round-tripping the agent.                        | iter8 commit 450a355: staged-diff was 1 new test file, 0 src/ files, 0 package.json, 0 CI workflows. Scorer verdict matched the trivial-risk profile. ~30s round-trip cost for a verdict the gate could have inferred from the diff stat.                                                                           | Recurring-class candidate (this is the first observation, so n=1; but the test-only no-src-change pattern is common). Mechanical-auto-ticket per P342 trust-boundary asymmetry — `/wr-itil:capture-problem` would create the ticket on next interactive turn. |
| improve | hook  | `~/.claude/.../external-comms-gate-marker-key.sh` (or equivalent)                                                                        | External-comms gate marker re-hashes on EVERY commit invocation per session subprocess, including the first one before any user-edit. Class is now stably recurring across iters 3/5/6/7/8 (n=5+). P023 already exists for the re-hash-after-edit subclass; needs an explicit Symptoms append for the per-subprocess case. | iter8 commit 450a355: first `git commit` blocked with `BLOCKED (external-comms gate / risk evaluator)` against an unedited body. Both gates PASS first-try. This is not a re-hash-after-edit (P023's original symptom) but a fresh-subprocess re-hash, which P023's Root Cause Analysis should be updated to cover. | appended to P023 update queue (existing ticket — instance count this session ≥5). Mechanical-auto-ticket-update on next interactive pass via `/wr-itil:manage-problem update P023`.                                                                           |

## No Action Needed

- T7 test landed green on first run (`npx vitest run test/cli.exposure-aware-soak-regression.test.js` → 2/2 passed, ~448ms). Both arms (`exposureAwareSoak: true` and default-OFF) pinned the contract per RFC-002 §Tasks T7 verbatim.
- Full `npm run prepush` green: 363 tests across 92 files + e2e CLI tests + audit:ci clean. No coverage / format / type / lint regressions.
- Architect + JTBD gates ran in parallel pre-write — both PASS first-try with no `AskUserQuestion` fires. The architect's note that "the realistic-payload + real-formatter axes are load-bearing for T7" (not duplicating `src/print-outdated.test.js:163-219`) confirmed the test placement and shape choice.
- ADR-0020 narrow exception followed correctly — test placed at `test/cli.exposure-aware-soak-regression.test.js` mirroring `test/cli.overrides-hygiene-regression.test.js` (RFC-001 T7 precedent).
- Commit-message body passed BOTH external-comms gates (risk + voice-tone) on first review — no body-edit loop needed.
- Pipeline risk-scorer verdict was commit=2/25 (Very Low) for the test-only change — confirmed risk-neutral.

## Context Usage (Cheap Layer)

| Bucket             | Bytes                            | % of total | Δ vs prior                     |
| ------------------ | -------------------------------- | ---------- | ------------------------------ |
| memory             | 389312                           | 41.9%      | +0% vs iter7 (389312 → 389312) |
| decisions          | 270092                           | 29.1%      | +0% vs iter7 (270092 → 270092) |
| problems           | 189422                           | 20.4%      | +0% vs iter7 (189422 → 189422) |
| jtbd               | 42306                            | 4.6%       | +0% vs iter7 (42306 → 42306)   |
| briefing           | 29265                            | 3.1%       | +0% vs iter7 (29265 → 29265)   |
| project-claude-md  | 7747                             | 0.8%       | +0% vs iter7 (7747 → 7747)     |
| hooks              | not measured — source-absent     | n/a        | n/a                            |
| skills             | not measured — source-absent     | n/a        | n/a                            |
| framework-injected | not measured — no on-disk source | n/a        | n/a                            |

**Total measured**: 928 144 bytes (unchanged from iter7 — this iter added one test file under `test/` which is not measured by any cheap-layer bucket).

Per-plugin breakdown available in /wr-retrospective:analyze-context (deep layer).

**Top-5 offenders** (unchanged from iter7):

1. memory — 389 312 bytes (auto-memory directory at `~/.claude/projects/-Users-tomhoward-Projects-dry-aged-deps/memory/`).
2. decisions — 270 092 bytes (`docs/decisions/` MADR 4.0 ADR set).
3. problems — 189 422 bytes (`docs/problems/` ticket bodies).
4. jtbd — 42 306 bytes (`docs/jtbd/` persona + job files).
5. briefing — 29 265 bytes (`docs/briefing/` topic files + index).

Deep analysis NOT recommended this retro — bucket counts unchanged from iter7 (0% deltas), all well below the +20% trigger and the 200K cheap-layer envelope.
